* 顺序容器 
  * 拥有由单一类型元素组成的一个有序集合
  * 两个主要顺序容器是 list 和 vector

* 关联容器
  * 支持查询一个元素是否存在，并且可以有效获取元素
  * 两个基本的关联容器类型 map(映射) 和 set(集合)
  * map是一个键值对，键用于查询，值包含我们希望使用的数据
  * set 包含一个单一键值，有效支持关于元素是否存在
  * map使用在，希望通过钥匙，取得密码箱中的数据的情况
  * set使用在，用于确认某一成员是否存在于集合之中

# vector VS list

* vector
  * 表示一段连续的内存区域，每个元素被顺序存储在这段内存中
  * 对vector的随机访问效率很高(对应的是顺序访问)
  * 插入删除任意元素的效率很低
* list
  * 表示非连续的内存区域，并通过一对指向首尾元素的指针双向链接器起来
  * 插入删除任意元素的效率很高
  * 对随机访问效率很低
* 选择准则
  * vector对应的顺序表 list对应链表
  * 如果我们需要随机访问一个容器 则vector要比list好得多
  * 如果我们已知要存储元素的个数 则vector又是一个比list好的选择
  * 如果我们需要的不只是在容器两端插入和删除元素 则list显然要比vector好
  * 除非我们需要在容器首部插入和删除元素 否则vector要比deque好 

# 定义一个顺序容器

```c++
#include <vector> 
#include <list> 
#include <deque> 
#include <map> 
#include <set> 

//定义
//svec是一个内含string对象的主vector 
//以及ilist是一个int型对象的空list 
//svec 和ilist 都是空的
vector< string > svec;
list< int > ilist;
//插入元素
//每次从标准输入读取一个字符串放到text_word中
//然后push_back()冉将 text_word 字符 串的拷贝插入到svec中
string text_word; 
while ( cin >> text_word ) 
	svec.push_back( text_word );
int ia[ 4 ] = { 0, 1, 2, 3 }; 
for ( int ix = 0; ix < 4; ++ix ) 
	ilist.push_back( ia[ ix ] ); 
```

# 迭代器 （iterator）

* 迭代器提供了一种一般化的方法，对顺序或关联容器类型中的每个元素进行连续访问

* 每种容器类型都提供一个begin()和end()成员函数
  * begin()返回一个iterator,指向容器的第一个元素
  * end()返回一个iterator，指向容器的末元素的下一个位置
* 定义

```C++
vector<string> vec;
vector<string>::iterator iter = vec.brgin();
vector<string>::iterator iter_end = vec.end(); 

```

* 使用

```c++
//代表vec中第一个元素
*iter;
// iterator算术论算只适用于vector或deque 而不适用于list 因为list的元素在内存中不 是连续存储的
//将iter 指向vec的中间元素
vector<string>::iterator iter = vec.begin()+vec.size()/2;
//将iter 向前移动两个元素
iter += 2; 
 // 用 svec 的全部元素初始化svec2 
vector<string> svec; 
vector<string> svec2( svec.begin(), svec.end() );
```

# 顺序容器操作

* push_back(元素)
  * 在顺序容器尾部插入单个元素

* insert(位置(iterator)，元素)
  * 在顺序容器的指定位置插入单个元素
* find(起始位置，结束位置，目标元素)
  * 返回被查找元素在容器中的位置

* erase(位置(iterator)) 
  * 删除单个元素
* srase(起始位置(iterator)，结束位置(iterator))
  * 删除指定范围的元素
* pop_back()
  * 删除容器的末元素
* swap(容器)
  * 将目标容器拷贝到当前容器

# map

* 在使用map 和set 时 两个最主要的动作是向里面放入元素 以及查询元素是否存在
* 定义

```c++
//为定义map对象 我们至少要指明键和值的类型
//定义了map对象word_count 它由string作为索引 并拥有一个相关的int值
map<string, int> word_count;
//定义了map对象word_count 它由string作为索引 并拥有一个相关的int值
class employee; 
map<int, employee*> personnel;
```

* 加入键/值元素对

```C++
//在word_count的键中进行寻找Anna，如果没有，添加一个新的键值对元素到对象中
//将键对应的值修改为1
word_count[ string("Anna") ] = 1;
//在map对象中添加单个元素
word_count.insert( 
 map<string,int>:: value_type( string("Anna"), 1 ) 
);
```

* value_type 表示相关联的键值对

```c++
//其作用是创建一个pair对象 接着将其直接插入map
map< string,int >:: value_type( string("Anna"), 1 ) 
```

* 查找并获取map中的元素
  * count(Keyvalue) 返回map中keyValue出现的次数
  * find(keyValue) 返回keyValue的iterator，如果不存在就返回end()的iterator

```c++
int count = 0;
map<string,int> word_count;
map<string,int>::itrerator it = word_count.find("wrinkles");
if(word_count.count("wrinkles"))
    count = word_count.count("wrinkles");
if ( it != word_count.end() ) 
	count = (*it).second; 

```

* 删除元素
  * 删除单个元素  erase(键值/iterator)
  * 删除一列元素  erase(起始iterator，结束iterator)