# 表达式

* 是C++程序中。最小程序单位，以分号;为结尾的一个完整语句

* 一个表达式由一个或多个操作数构成，操作数包括变量和常量

* 在多个操作数组成的表达式中通过操作符将操作数作用在一起

* 表达式可以分为

  * 变量语句

  * 变量对象定义语句

  * 赋值语句
  * 逻辑语句
  
* 操作符根据作用在几个操作数上可以分为

  * 一元操作符 & * ^ ! ~  ....
  * 二元操作符 +-*/ ...

* 复合表达式

  * 由两个或以上的操作符组合起来的表达式

  ```c++
  //prt的值是否为0判断 并上 prt地址是否为0判断
  (ptr != 0) && (*prt !== 0)
  ```

  # new和delete表达式

  * 空闲存储区
  * 堆

  ```c++
  //从空闲存储区中分配一个int类型的对象 并用的地址初始化指针pi
  int *pi = new int(1024)
  ```

  

  ## 内容管理

  ##  动态管理