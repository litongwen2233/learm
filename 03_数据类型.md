# 数据类型

* C++内置的一组数值类型
  * 字符型char
  * 整型int 短整型short 长整型long
  * 浮点型 float 双精度double 长双精度 long double

# 文字常量

* 当一个具体的数值 例如 1 ，出现在程序中，被称为文字常量
* 值不能被改变
* 每个常量都有相对的类型 比如 0是int类型 3.1415是double类型
* 在整型常量前面加上0 代表这个常量是八进制的 前面0x的是16进制的常量常量
* 用''或者“”包含起来的数值或字符代表的是字符型常量或字符串常量

# 变量

* 变量就是我们定义的一个指定类型和名称的数值，我们可以对其值进行操作
* 变量的本质是我们指定了一个变量名指向了内存中的指定区域 可以通过程序对其进行读写等操作

# 变量名

* 由字母，数字和下划线组成，只能以字母或下划线开头
* 不能使用C++中的关键字作为变量的变量名

# 对象的定义

* 对象是某一种数据类型的一个变量，是类的具体实现

* 简单对象定义

```c++
int year;
int month;
```

* 全局变量
  * 定义在类，函数和代码块外的变量
  * 如果不赋初始值，系统会赋值为0
* 局部变量
  * 通过new表达式动态分配的变量
  * 在函数和代码块中的变量
  * 如果没有在定义的时候进行赋值，初始变量的值就是随机的
* 成员变量
  * 在类内部定义的变量

# 指针类型

* 在上述的类型进行命名的标识符前面加一个*，来定义一个指针

```c++
//定义了一个int类型的指针 指向了空
int *p = NULL;
//将一个int类型的变量的地址赋值给int类型指针
int ival = 1024；
int *p2 = &ival;
```

* *解引用 在指针初始化的时候表示这是一个指针，在赋值过程中表示取指针所指地址中实际的数据
* &在赋值过程中 表示该变量对应的地址

# 字符串类型

* c风格字符串
  * 字符串被存储在字符数组中，一般通过一个char*类型的指针类操纵它
  *  标准C库  为操纵C风格的字符串提供了一组函数

```c++
// 返回字符串的长度  
int strlen( const char* ); 
// 比较两个字符串是否相等  
int strcmp( const char*, const char* );  
// 把第二个字符串拷贝到第一个字符串中  
char* strcpy(char*, const char* ); 
...
```

* string类 头文件 <string>

  * 定义

  ```c++
  #include<string>
  //初始化一个空的字符串
  string st0;
  //初始化一个字符串st 内容为THe expence of spirit\n
  string st("THe expence of spirit\n");
  //用st来初始化st2
  string st2(st);
  ```

  * 长度获取

  ```c+
  //长度由size操作返回
  cout<<"The size of"
  	<<"st"
  	<<"is"<<st.size()
  	<<"characters, including the newline\n";
  ```

  * 判断是否为空

  ```C++
  if(st.empty())
  {
      ;//空
  }
  ```

  * 拷贝

  ```c+
  //将st2中的内容替换成为st3中的内容、
  st2 = st3;
  ```

  * 串联

  ```c+
  //将两个字符串的内容拼接到一起
  //将s1和s2 拼接后 赋值给s3
  string s3 = s1 + s2;
  //将s2的内容拼接到s1后面
  s1 += s2；
  ```

  * string类型能够自动的将C风格的字符串转换成string对象

  ```c++
  string s1; 
  const char *pc = "a character array"; 
  s1 = pc; // ok 
  ```

  * string类型要转为为C风格的字符串 需要显式的调用c_str()操作

  ```c+
  string s1("the is C++"); 
  const char *str = s1.c_str(); // ok 
  ```

# const限定修饰符

*  const类型限定修饰符提供了一个解决方案 它把一个对象转换成一个常量

```c+
const int bufSize = 512 // 缓冲区大小
```

* 定义bufSize 是一个常量 并将其初始化为512 在程序中任何改变这个值的企图都将导  致编译错误 因此 它被称为是只读的 read- only 
* 常量在初始化的时候必须赋值 在定义之后值不能被修改
* 一个非const对象的指针指向一个常量  对象 的动作都将引起编译错误

# 引用类型

* 引用 reference 有时候又称为别名 alias 它可以用作对象的另一个名字 通过引  用我们可以间接地操纵对象 使用方式类似于指针
* 引用主要被用作函数的形式参数——通常将类对象传递给一个函数
* 引用类型由类型标识符和一个取地址操作符来定义 引用必须被初始化

```c++
int ival = 1024; 
// ok: refVal 是一个指向ival的引用 
int &refVal = ival;   
// 错误 引用必须被初始化为指向一个对象  
int &refVal2;
```

* 一旦引用已经定义 它就不能再指向其他的对象

# 容器类型 \#include< vector > 

*   vector类为内置数组提供了一种替代表示
* 定义

```C+
vector < int > ivec(10);  //int ia[10];
```

* vector可以被另一个vector初始化 或被赋给另一个vector 

