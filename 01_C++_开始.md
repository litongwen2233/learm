```md
以下内容均是通过C++ Primer进行学习的 有兴趣可以去阅读原文
```

# C和C++的不同

```md
C++是带类的C 
```

* c++和c最大的不同的地方在于 c++是面向对象开发的 而c是面向过程开发的
  
  * 面向过程
    
    * **"怎么做"——以"动作"为中心，关注解决问题的步骤**
    * **基本单元**：函数
    * **核心思想**：将程序分解为一系列**顺序执行的步骤或函数**，数据在函数之间传递
  
  * 面向对象
    
    * **"是什么"——以"对象"为中心，关注谁来做这件事**
    * **基本单元**：类（Class）和对象（Object）
    * **核心思想**：将程序分解为**相互交互的对象**，每个对象包含**数据（属性）**和**操作数据的方法**

# 语句

* 在C++中动作被称为表达式，以分号`;` 为结束是一个完整的语句

* 语句就是C++中最小的程序单元

```c++
int Book_count = 0 ;
// int 为变量类型
// Book_count 是我们定义的一个变量
//这里的含义就是我们声明了一个叫Book_count的变量 并赋值为0
```

# 函数

* 定义 
  
  * 函数返回值类型  函数名(参数列表...)  { 函数内容 }

* main函数 每一个程序有且仅只能有一个main函数 这里程序的入口

```c++
int add(int a,int b)
{
    int result = a+b;
    return result;   
}
```

# 控制语句

## 判断语句

* if-else
    if(判断表达式)
    {
  
        执行语句
  
    }
    else if(判断表达式)
    {
  
        执行语句
  
    }
    else
    {
  
        执行语句
  
    }

* switch

```c++
switch(变量)
{
    case 常量1:
    break;
    case 常量2:
    break;
    ...
}
```

## 循环语句

* for

```c++
for(循环变量初始化;循环判断表达式;循环变量改变表达式)
{
    循环体
}
```

* while

```c++
while(循环判断表达式)
{
    循环体
}
```

# 标准输入输出

* 输入

```c++
#include<iostream>
int main()
{
    string file_name;
    cout << "Please enter input and optput file names:"
    //将用户在终端终端输入赋值到file_name
    cin >> file_name;
}
```

* 输出

```c++
#inlclude<iostream>
int main()
{
    //输出操作符<<用来将一个值导向到标准输出 cout 或标准错误 cerr 上
    //cout << '\n' == cout << endl
    cout << "helloc" << endl;
    return 0;
}
```


