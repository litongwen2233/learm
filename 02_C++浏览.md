# 动态内存分配

* 静态内存分配
  
  * 编译器在处理程序源代码时分配
  
  * 静态内存分配是在程序执行之前进行的 因而效率比较高
  
  * 在程序执行前就被知道类型和数量
  
  * 基本类型都是

  * 有名字的变量 我们直接对其进行操作
  
* 动态内存分配
  
  * 程序执行时调用运行时刻库函数来分配
  
  * 由程序员显式的管理 通过new和delete两个表达式完成
  
  * 没有名字的变量 通过指针间接的进行操作
  
  ```c++
  //分配了一个没有名字的 int 类型的对象 对象初始值为 2048 
  //然后 表达式返回对象在内存中的地址
  //接着 这个地址被用来初始化指针对象 pint 
  //对于动态分配的内存 惟一的访问方式是通过指针间接地访问
  int *pint = new int(2048);
  //分配一个含有四个整数元素的数组
  int *pint2 = new[ 4 ];
  //删除单个对象
  delete pint;
  //删除一个对象数组
  delete [] pint2;
  ```

# 基于对象的设计

* 先实现一个抽象基类 在对基类进行具体实现
  * 确定基类应该提供哪些操作

* 内联函数
  
  * 在类的定义中被定义的成员函数会被自动当作是内联函数
  
  * 我们也可以用 inline 关键字显式地要求一个函数被视为内联函数

  * 内联函数在它的调用点上被展  开 一般来说 内联函数不会引入任何函数调用
  
* 构造函数
  
  * 构造函数与类相同的名字
  
  * 专门用于初始化对象
  
  * 类的每个对象第一次被使用之前 这构造函数就被自动应用在对象上

* 析构函数

  * 析构函数函数名是在对应的类名前面加上~

  * 专门用来处理类的注销

* 重载函数

  * 允许多个函数使用相同的函数名 但是函数的参数表必须不同(参数类型不同或数量不同)

  

```c++
//定义一个类 类名为IntArray
class IntArray 
{
    //公有操作集合
    //实现了这个类的行为的操作集合 它包括该类的所有成员函数 或者只包括其中一个子集
    public:
        //构造函数(函数名和类名相同) 三种不同的重构构建函数 对应以不同的方式来实现类的初始化
        explicit IntArray( int sz = DefaultArraySize );
        IntArray( int *array, int array_size );
		IntArray( const IntArray &rhs );
    	//析构函数 在类对象被程序最后一次使用之后 自动调用
    	~InintArray();
        // 相等与不相等操作 
        bool operator==( const IntArray& ) const;
        bool operator!=( const IntArray& ) const;
        // 赋值操作符 
        IntArray& operator=( const IntArray& );
        int size() const; // 
        void sort(); // 
        int min() const; // 
        int max() const; // 
        // 如值在数组中找到
        // 返回第一次出现的索引
        // 否则返回-1
        int find( int value ) const; // 
    //私有实现代码
    //私有成员提供私有 实现代码 ——即存储信息的数据
    private:
        // 内部数据
        int _size;
        int *ia;
};
//定义一个IntArray类的对象
IntArray myArray;
//指向IntArray类对象的指针
IntArray *pArray = new IntArray;
// 用 myArray 数组中的最小元素来初始化 min_val
int min_val = myArray.min();
//在动态分配的 IntArray 对象中查找最大值
int max_val = pArray->max();
```

# 面向对象的设计

* 继承机制

  * 当一个子类继承一个父类(基类)时，子类可以访问基类的数据成员和成员函数

  调用基类和子类

  ```c++
  #include<InitArray.h>
  void swap( InitArray &ia,int i,int j )
  {
      int tmp = ia[i];
      ia[i] = ia[j];
      ia[j] = tmp;
  }
  //调用
  // ok ia是一个IntArray 
  swap( ia, 0, 10 ); 
  // ok: iarc 是IntArray的子类型 
  swap( iarc, 0, 10 ); 
  // ok: ias 也是IntArray的子类型 
  swap( ias, 0, 10 ); 
  // error: string 不是IntArray的子类型 
  string str( "not an IntArray!" ); 
  swap( str, 0, 10 );
  ```

* 虚拟函数

  * 在不同的子类中会继续不同具体实现的函数，我们会把这些函数标记为virual(虚拟的)

# 泛型设计

* 在C++中提供的一种能够对于不同类型通用的类设计方式
* template

```c++
// class 代表参数是一个类型
//elemType 代表实际的参数名 在类的定义中使用 作为实际类型的占位符
template < class elemType >
class Array {
    public:
    	//把元素类型参数化
    explicit Array( int size = DefaultArraySize ); 
	Array( elemType *array, int array_size ); 
	Array( const Array &rhs ); 
	virtual ~Array() { delete [] ia; } 
	bool operator==( const Array& ) const; 
	bool operator!=( const Array& ) const;
}
```

具体使用

```c++
#include <iostream> 
#include "Array.h" 
int main() 
{ 
const int array_size = 4; 
 // elemType 变成了int 
 Array<int> ia(array_size); 
 // elemType 变成了double 
 Array<double> da(array_size); 
 // elemType 变成了char 
 Array<char> ca(array_size); 
 int ix; 
for ( ix = 0; ix < array_size; ++ix ) { 
ia[ix] = ix; 
da[ix] = ix * 1.75; 
ca[ix] = ix + 'a'; 
 } 
for ( ix = 0; ix < array_size; ++ix ) 
cout << "[ " << ix << " ] ia: " << ia[ix] 
    << 
"\tca: " << ca[ix] 
<< "\tda: " << da[ix] << endl; 
 return 0; 
}
```

* 实例声明就是在类模板名的后面加上一对`<> `并在<>中添加实际的类型

# 基于异常的设计

* 异常 exception 是指在运行时刻程序出现的反情形 例如数组下标越界 打开文件失  败以及可用动态内存耗尽等等
* 异常处理 exception handling 为 响应运行时刻的程序异常 提供了一个标准的语言  级的设施 它支持统一的语法和风格 也允许每个程序员进行微调
* 构成
  * 异常出现点
  * 程序中异常被处理的点

```c++
int *pstats = new int[ 4 ];
try 
{
    pstats[ 0 ] = sum_it( ia, size ); 
    pstats[ 1 ] = min_val( ia, size ); 
	pstats[ 2 ] = max_val( ia, size );
}
catch( string exceptionMsg ) 
	{
    /* 处理异常的代码 */
	}  
catch( const statsException &statsExcp ) 
	{/* 处理异常的代码 */} 
```

# 向量

* 容器抽象 vector
* 是在标准C++标准库中，制定的一个泛型的数组类

```c++
//定义一个包括10个整型对象向量
vector<int> ivec(10);
//定义一个包含10个字符串对象的向量
vector<string> svec(10);
```

