

# 概念

* 设备树

  * 负责描述板子上“有什么”设备、地址在哪、状态如何

  * 设备树（DTS/DTB）是一份“硬件资源清单”

  * 设备树采用树状节点结构，用属性（键值对）描述硬件，核心是解决“硬件在哪、是什么”的问题

  * | 属性名                       | 描述作用                           |
    | ---------------------------- | ---------------------------------- |
    | compatible                   | 设备的身份证，驱动匹配的关键纽带   |
    | reg                          | 设备寄存器地址及大小（内存映射）   |
    | interrupts/ interrupt-parent | 中断号、触发方式及所属中断控制器   |
    | clocks/ pinctrl-\*           | 时钟源引用与引脚复用配置           |
    | status                       | 设备启用状态                       |
    | gpios                        | 引用的 GPIO 引脚                   |
    | 拓扑信息                     | CPU 数量、内存基地址、总线挂载关系 |
    
  * 设备树示例
  
  ```dt
  /dts-v1/; //表示设备树源文件采用 DTS 语法版本 1，这是目前唯一使用的版本，必须位于文件开头
  / {//设备树的起点，整个树只有一个根节点，用 / 表示 
  	//根节点的属性
  	//compatible：描述板卡兼容性，驱动匹配的关键标识
      compatible = "mycompany,myboard";
       /* 
       * #address-cells 和 #size-cells 解读：
       * - #address-cells = <1>：子节点 reg 中地址占 1 个 cell（32 位）
       * - #size-cells = <1>：子节点 reg 中大小占 1 个 cell（32 位）
       * 如果改成 #address-cells = <2>，则地址需要写两个 cell
       * 例如：reg = <0 0x80000000 0 0x10000000>（64 位地址）
       */
      #address-cells = <1>;
      #size-cells = <1>;
  	//内存节点（memory@80000000） 描述系统物理内存布局
  	//device_type = "memory"：固定值，标识该节点为内存设备 
  	//reg = <0x80000000 0x10000000>：表示内存起始地址为 0x80000000，大小为 0x10000000（256MB）。
  	//地址和长度各占 1 个 cell，由父节点的 #address-cells = <1> 和 #size-cells = <1> 决定
      memory@80000000 {
          device_type = "memory";
          reg = <0x80000000 0x10000000>;
      };
  	//串口节点（uart0: serial@1c000000）
      uart0: serial@1c000000 {
          compatible = "ns16550";
          reg = <0x1c000000 0x1000>;
          interrupt-parent = <&intc>;
           /*
           * interrupts 属性解读（对于 GIC 中断控制器）：
           * 第一个数字：中断类型
           *   0 = SPI（共享外设中断）
           *   1 = PPI（私有外设中断）
           * 第二个数字：中断号（相对于中断类型的偏移）
           * 第三个数字：触发方式
           *   1 = 上升沿触发
           *   2 = 下降沿触发
           *   4 = 高电平触发
           *   8 = 低电平触发
           */
          interrupts = <0 5 4>;
          status = "okay";
      };
  	//中断控制器节点（intc: interrupt-controller@1f000000）
      intc: interrupt-controller@1f000000 {
          compatible = "arm,gic-v3";
          reg = <0x1f000000 0x10000>;
          interrupt-controller;
          #interrupt-cells = <3>; /* 每个 interrupts 属性占 3 个 cell */
      };
          /* 
       * pinctrl 引脚复用配置示例
       * 实际开发中，pinctrl 节点通常由芯片厂商提供或由设备树包含
       */
      pinctrl: pinctrl {
          uart0_pins: uart0-pins {
              pins = "PB0", "PB1";
              function = "uart";
          };
      };
  
      /* 自定义 LED 节点 */
      myled {
          compatible = "mycompany,myled";
          status = "okay";
          label = "status-led";
          gpios = <&gpio1 5 GPIO_ACTIVE_LOW>;  /* GPIO1 的第 5 个引脚，低电平有效 */
      };
  };
  ```
  
* 驱动

  * 驱动（.c 代码）是“操作逻辑实现”，负责描述“怎么弄”这些设备（如何初始化、读写、处理中断等）
  
  * 专注于实现操作逻辑，并通过内核 API 从设备树动态提取资源
  
  * 搭建驱动骨架与功能回调 将内核中的标准设备函数进行实现
  
  * 声明匹配表 定义 `of_match_table`，填入与设备树节点 `compatible` **完全一致**的字符串。内核启动时会遍历设备树节点，匹配成功才触发驱动的 `probe` 函数
  
  * 在 `probe` 中提取资源并初始化
  
  * 驱动示例代码

```c
/*
 * myled_drv.c - 基于设备树的平台驱动示例
 * 功能：通过 /dev/myled 控制 LED 的开关
 * 支持：应用层通过 write() 写入 '1' 或 '0' 控制 LED
 */

#include <linux/module.h>
#include <linux/init.h>
#include <linux/platform_device.h>
#include <linux/of.h>
#include <linux/of_gpio.h>
#include <linux/gpio.h>
#include <linux/fs.h>
#include <linux/cdev.h>
#include <linux/device.h>
#include <linux/uaccess.h>

/* ===================== 设备结构体定义 ===================== */

/**
 * struct myled_dev - 自定义 LED 设备结构体
 * @devid:    设备号（主设备号 + 次设备号）
 * @cdev:     字符设备结构体
 * @class:    设备类（用于自动创建设备节点）
 * @device:   设备节点指针
 * @gpio:     从设备树获取的 GPIO 编号
 */
struct myled_dev {
    dev_t devid;                /* 设备号 */
    struct cdev cdev;           /* 字符设备结构体 */
    struct class *class;        /* 设备类 */
    struct device *device;      /* 设备节点 */
    int gpio;                   /* GPIO 编号 */
};

static struct myled_dev myled; /* 全局设备结构体实例 */

/* ===================== 文件操作接口 ===================== */

/**
 * myled_open() - 当应用层调用 open("/dev/myled") 时执行
 * @inode: inode 结构体指针
 * @filp:  file 结构体指针
 * 返回值：成功返回 0
 */
static int myled_open(struct inode *inode, struct file *filp)
{
    pr_info("myled: device opened\n");
    return 0;
}

/**
 * myled_write() - 当应用层调用 write(fd, buf, count) 时执行
 * @filp:  file 结构体指针
 * @buf:   用户空间传入的数据缓冲区
 * @cnt:   要写入的字节数
 * @offt:  文件偏移量
 * 返回值：成功返回写入的字节数，失败返回负的错误码
 * 
 * 用户写入 '1' 点亮 LED，写入 '0' 熄灭 LED
 */
static ssize_t myled_write(struct file *filp, const char __user *buf,
                           size_t cnt, loff_t *offt)
{
    char value;

    /* 从用户空间拷贝数据到内核空间 */
    if (copy_from_user(&value, buf, 1))
        return -EFAULT;

    /* 根据用户传入的数据控制 LED */
    if (value == '1') {
        gpio_set_value(myled.gpio, 1);   /* 点亮 LED */
        pr_info("myled: LED ON\n");
    } else if (value == '0') {
        gpio_set_value(myled.gpio, 0);   /* 熄灭 LED */
        pr_info("myled: LED OFF\n");
    } else {
        return -EINVAL;  /* 无效输入 */
    }

    return cnt;  /* 返回实际写入的字节数 */
}

/**
 * myled_release() - 当应用层调用 close(fd) 时执行
 * @inode: inode 结构体指针
 * @filp:  file 结构体指针
 * 返回值：成功返回 0
 */
static int myled_release(struct inode *inode, struct file *filp)
{
    pr_info("myled: device released\n");
    return 0;
}

/**
 * file_operations 结构体：应用层操作与驱动函数的映射表
 * 内核 VFS 层通过此结构体将系统调用分发给对应的驱动函数
 */
static struct file_operations myled_fops = {
    .owner   = THIS_MODULE,    /* 模块所有者 */
    .open    = myled_open,     /* open 系统调用对应的驱动函数 */
    .write   = myled_write,    /* write 系统调用对应的驱动函数 */
    .release = myled_release,  /* close 系统调用对应的驱动函数 */
};

/* ===================== 平台驱动核心函数 ===================== */

/**
 * myled_probe() - 匹配成功时自动调用 [1](@ref)
 * @pdev: 匹配到的平台设备指针
 * 返回值：成功返回 0，失败返回负的错误码
 * 
 * 该函数完成以下工作：
 * 1. 解析硬件资源：从设备树获取 GPIO 编号等信息
 * 2. 注册字符设备：向内核注册名为 myled 的字符设备
 * 3. 创建设备节点：在 /dev/ 目录下创建 myled 设备文件
 */
static int myled_probe(struct platform_device *pdev)
{
    struct device *dev = &pdev->dev;
    struct device_node *np = dev->of_node;
    int ret;

    pr_info("myled: driver and device matched!\n");

    /* ===== 第一步：解析硬件资源（从设备树获取 GPIO） ===== */
    myled.gpio = of_get_named_gpio(np, "gpios", 0);
    if (!gpio_is_valid(myled.gpio)) {
        dev_err(dev, "failed to get gpio\n");
        return -EINVAL;
    }

    /* 申请 GPIO 资源，防止其他模块占用 */
    ret = gpio_request(myled.gpio, "myled");
    if (ret) {
        dev_err(dev, "failed to request gpio\n");
        return ret;
    }

    /* 设置 GPIO 方向为输出，默认输出高电平（点亮 LED） */
    gpio_direction_output(myled.gpio, 1);
    dev_info(dev, "LED initialized on GPIO %d\n", myled.gpio);

    /* ===== 第二步：注册字符设备 ===== */
    /* 2.1 动态分配设备号（主设备号 + 次设备号） */
    ret = alloc_chrdev_region(&myled.devid, 0, 1, "myled");
    if (ret < 0) {
        dev_err(dev, "failed to alloc chrdev region\n");
        goto free_gpio;
    }

    /* 2.2 初始化字符设备，绑定 file_operations */
    cdev_init(&myled.cdev, &myled_fops);
    myled.cdev.owner = THIS_MODULE;

    /* 2.3 向内核添加字符设备 */
    ret = cdev_add(&myled.cdev, myled.devid, 1);
    if (ret < 0) {
        dev_err(dev, "failed to add cdev\n");
        goto unregister_region;
    }

    /* ===== 第三步：创建设备节点（/dev/myled） ===== */
    /* 3.1 创建设备类，用于在 /sys/class/ 下注册 */
    myled.class = class_create(THIS_MODULE, "myled");
    if (IS_ERR(myled.class)) {
        ret = PTR_ERR(myled.class);
        dev_err(dev, "failed to create class\n");
        goto del_cdev;
    }

    /* 3.2 在类下创建设备，自动生成 /dev/myled 节点 */
    myled.device = device_create(myled.class, NULL, myled.devid,
                                  NULL, "myled");
    if (IS_ERR(myled.device)) {
        ret = PTR_ERR(myled.device);
        dev_err(dev, "failed to create device\n");
        goto destroy_class;
    }

    dev_info(dev, "myled probe successfully: gpio=%d, major=%d, minor=%d\n",
             myled.gpio, MAJOR(myled.devid), MINOR(myled.devid));
    return 0;

    /* ===== 错误处理：逐级回滚已申请的资源 ===== */
destroy_class:
    class_destroy(myled.class);
del_cdev:
    cdev_del(&myled.cdev);
unregister_region:
    unregister_chrdev_region(myled.devid, 1);
free_gpio:
    gpio_free(myled.gpio);
    return ret;
}

/**
 * myled_remove() - 模块卸载时自动调用
 * @pdev: 平台设备指针
 * 返回值：成功返回 0
 * 
 * 逆序释放 probe 中申请的所有资源
 */
static int myled_remove(struct platform_device *pdev)
{
    /* 逆序释放 probe 中申请的资源 */
    device_destroy(myled.class, myled.devid);    /* 销毁设备节点 */
    class_destroy(myled.class);                  /* 销毁设备类 */
    cdev_del(&myled.cdev);                       /* 删除字符设备 */
    unregister_chrdev_region(myled.devid, 1);    /* 释放设备号 */
    gpio_set_value(myled.gpio, 0);               /* 熄灭 LED */
    gpio_free(myled.gpio);                       /* 释放 GPIO 资源 */

    pr_info("myled: driver removed!\n");
    return 0;
}

/* ===================== 设备树匹配表 ===================== */

/**
 * myled_of_match[] - 设备树 compatible 匹配表
 * 内核通过此表将设备树节点与驱动进行匹配
 * 最后一个元素必须为哨兵（空结构体）
 */
static const struct of_device_id myled_of_match[] = {
    { .compatible = "mycompany,myled" },  /* 与设备树节点中的 compatible 对应 */
    { /* sentinel */ }                     /* 哨兵，表示匹配表结束 */
};

/* 定义 MODULE_DEVICE_TABLE，使内核模块支持设备树自动匹配 */
MODULE_DEVICE_TABLE(of, myled_of_match);

/* ===================== 平台驱动结构体 ===================== */

/**
 * myled_driver - platform_driver 结构体
 * 包含 probe、remove 函数指针和驱动名称、匹配表
 */
static struct platform_driver myled_driver = {
    .probe  = myled_probe,       /* 匹配成功时调用 */
    .remove = myled_remove,      /* 模块卸载时调用 */
    .driver = {
        .name = "myled",                    /* 驱动名称 */
        .of_match_table = myled_of_match,   /* 设备树匹配表 */
    },
};

/* ===================== 模块入口与出口 ===================== */

/**
 * module_platform_driver() - 简化 platform 驱动注册的宏
 * 展开后等价于 module_init + platform_driver_register
 * 和 module_exit + platform_driver_unregister
 */
module_platform_driver(myled_driver);

/* ===================== 模块元信息 ===================== */

MODULE_LICENSE("GPL");                    /* 许可证声明 */
MODULE_AUTHOR("Your Name");               /* 作者信息 */
MODULE_DESCRIPTION("Platform LED driver with device tree");  /* 模块描述 */
MODULE_ALIAS("platform:myled");           /* 模块别名，用于自动加载 */


```

* 配套 Makefile

```makefile
# ============================================================
# Makefile —— 编译 platform_led 驱动模块（Linux 内核模块）
# 适用平台：ARM 嵌入式 Linux（如 s3c2440）
# ============================================================

# 定义目标内核模块的名称（.ko 文件由 .o 文件生成）
# obj-m 告诉内核构建系统：将 platform_led.o 编译成可加载内核模块
obj-m := platform_led.o

# 定义内核源码目录路径
# KDIR 指向当前运行内核的构建目录，用于获取内核头文件、Makefile 和编译规则
KDIR := /home/topeet/Linux/linux_sdk/kernel

# 获取当前工作目录的绝对路径
# PWD 变量保存模块源码所在目录，供内核构建系统返回此处编译
PWD := $(shell pwd)

# 默认目标 all
# 执行 make 时，跳转到内核目录，使用内核的 Kbuild 系统编译外部模块
all:
	$(MAKE) -C $(KDIR) M=$(PWD) modules

# 清理目标 clean
# 跳转到内核目录，使用内核的 Kbuild 系统清理模块编译产物
clean:
	$(MAKE) -C $(KDIR) M=$(PWD) clean
```

* 应用层测试代码

```c
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <unistd.h>

int main(int argc, char *argv[])
{
    int fd;
    char value;

    if (argc != 2) {
        printf("Usage: %s <0|1>\n", argv;
        return -1;
    }

    /* 打开设备文件 */
    fd = open("/dev/myled", O_RDWR);
    if (fd < 0) {
        perror("open");
        return -1;
    }

    /* 写入控制值 */
    value = argv[0];
    write(fd, &value, 1);

    close(fd);
    return 0;
}
```



* 实现
  * 注册
  * 模块加载时，`module_platform_driver` 宏自动注册 platform 驱动，内核根据 `of_match_table` 中的 `compatible` 值与设备树节点匹配，匹配成功后自动调用 `myled_probe`，完成 GPIO 初始化、字符设备注册和 `/dev/myled` 节点创建
  * 调用
  * 应用层通过 `write()` 系统调用控制 LED 时，VFS 层根据 `file_operations` 结构体将请求分发给 `myled_write` 函数，该函数最终调用 `gpio_set_value` 操作硬件 GPIO 引脚

# 关系

* 内核在解析设备树后，根据compatible属性寻找匹配的驱动

* 设备树是一种用于描述硬件平台及其组件的数据结构和规范

  ，它以一种结构化的方式描述系统中的各种硬件设备、总线、中断控制器以及它们之间的连接关系

  。而驱动则是运行在操作系统上的软件模块，负责与硬件进行交互

# 匹配机制详细流程

```
内核启动 → 解析 DTB → 生成 platform_device 链表
                     ↓
模块加载 → platform_driver_register()
                     ↓
platform 总线执行 match() 函数
                     ↓
对比 driver.of_match_table 与 device.of_node.compatible
                     ↓
匹配成功 → 调用 driver.probe(myled_probe)
                     ↓
probe 中提取资源、注册字符设备、创建设备节点
                     ↓
应用层通过 /dev/myled 访问设备
```

# 常见问题

### 1. GPIO 编号从 0 开始

设备树中的 GPIO 编号从 0 开始，不是从 1 开始。例如 `gpios = <&gpio1 5 GPIO_ACTIVE_LOW>` 表示 GPIO1 的第 6 个引脚（编号 5）。

### 2. compatible 字符串命名规范

`compatible` 字符串的格式为 `"厂商,设备名"`，例如 `"mycompany,myled"`。这是为了确保不同厂商的同名设备不会冲突。

### 3. of_match_table 最后一个元素必须是空结构体

必须有一个空结构体作为哨兵，表示匹配表结束。如果缺少哨兵，内核会越界访问，导致不可预知的行为。



### 4. insmod 后没有出现 /dev/myled 的可能原因

- udev 未自动创建设备节点（需要 udev 规则支持）
- `class_create` 失败（检查内核配置）
- `device_create` 失败（检查设备号是否被占用）
- 设备树节点 `status` 属性不是 `"okay"`

### 5. 驱动加载后 dmesg 没有输出

- 检查 `printk` 日志级别是否被屏蔽
- 使用 `dmesg -n 8` 设置日志级别为最高
- 检查模块是否成功加载（`lsmod | grep myled`）