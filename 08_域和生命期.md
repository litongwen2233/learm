# 域

C++程序中的每个名字都必须指向惟一的一个实体。这并  不意味着在一个C++程序中 一个名字只能被使用一次 一个名字可以被重新使用以指向不  同的实体 只要编译器能够根据上下文 context 区分出该名字的不同含义即可

* 用于区分名字含义的一般上下文就是域
  * 上下文:与当前执行任务相关的环境和状态信息的集合
* 分类
  * 局部域:包含在函数定义中的程序文本部分
  * 名字空间域:程序的最外层的命令空间域
  * 类域:
* 同一个名字在不同域中可以引用不同的实体

# 全局对象和函数

# 局部对象

# 动态分配对象

# 命名空间定义

# 使用命名空间成员